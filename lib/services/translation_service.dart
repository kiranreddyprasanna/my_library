class TranslationService {
  Future<String> translate(String text, String targetLang) async {
    // Step 1: Online translation (Google/LibreTranslate)
    // Step 2: Add offline pack later
    return "$text (translated)";
  }
}
